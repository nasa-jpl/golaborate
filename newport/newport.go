/*Package newport provides an HTTP server for ESP and XPS series motion controllers.

This package could use some polish, the interfaces are asymmetric, where the
ESP implements more or only the HTTP adaptor, but the XPS implements a Go-first
implementation that is wrapped by the HTTP adapter.

*/
package newport
